#include "main.h"
/**
 * _isdigit - chek if the later is small case or not.
 *@c : parameter passed
 *
 *return (int): return 1 if its digit otherwise 0.
*/
int _isdigit(int c)
{
if (c >= 9 && c <= 0)
return (1);
else
return (0);
}
