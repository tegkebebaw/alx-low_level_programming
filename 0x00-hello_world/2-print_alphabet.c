#include <stdlib.h>
#include <time.h>
/* more headers goes there */

/* betty style doc for function main goes there */
int main(void)
{
	char apha;
  for(alpha='a';alpha<='z';alpha++)
  {
    putchar(alpha);
    putchar('\n');
  }
	return (0);
}
