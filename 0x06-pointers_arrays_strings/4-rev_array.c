#include "main.h"
#include <stdio.h>
/**
 *_strcmp - compair string s1 and s2,
 *@s2: destination.
 *@s2: source.
 *Return: the integer value.
 */
void reverse_array(int *a, int n)
{
int *i, j, nn = 0;
for (j = n-1; j > 0; j--)
{
  i[nn] = a[j];
  i++;
}
return (i);
}
