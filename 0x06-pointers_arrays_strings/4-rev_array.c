#include "main.h"
#include <stdio.h>
/**
 *reverse_array - compair string s1 and s2,
 *@a: destination.
 *@n: source.
 */
void reverse_array(int *a, int n)
{
int *i, j, nn = 0;
for (j = n-1; j > 0; j--)
{
i[nn] = a[j];
i++;
}
}
