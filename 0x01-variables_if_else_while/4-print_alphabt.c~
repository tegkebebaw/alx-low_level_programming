#include <stdio.h>
/* more headers goes there */

/* betty style doc for function main goes there */
int main(void)
{
	char alpha;
  for(alpha = 'a';alpha <='z';alpha++)
  {
    if(alpha=='e' || alpha =='e')
    {    
    }
    else
    {
      putchar(alpha);
      putchar('\n');
    }
  }
	return (0);
}
