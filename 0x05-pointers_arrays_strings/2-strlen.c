#include "main.h"
#include <string.h>
/**
 * _strlen - length of
 *return : return length of character
 * @s : parameter 1.
 *return - return length of character
 *strlen : is function to determine length of string
 */
int _strlen(char *s)
{
return (strlen(s));
}
